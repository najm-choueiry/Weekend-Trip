password (Password should contain 8 characters minimum, one special character minimum, at least one upper case letter,  etc. )


email (should have also the email format: @, no dashes, a "dot" somewhere after the @. Store the entered fields in a JSON object. )

id: add 10 numbers ((using prompt()) and implement Merge Sort algorithm using JS. )

input a string (JS will display on the DOM if this string is palindrome or not. The function should be implemented recursively. )


year of birth (the system will display if the age is prime or not. )


//

Write a class using JS that represents a course and ask build a form that takes values from the user to construct an object of that class from it (using constructor). 
//


enter a string and reverse the letters in that string.The string might not contain letters. 


input a string. First, move the first set of consonants (one or more) to the end of the word. Second, add “ay” to the end of the word. For example, the word “banana” would become “ananabay.”


[Implement some animations of your choice using JS (not CSS). ]


Get your IP address using JS (automatically) and sum the even numbers in it. 

Display your current location in a <h3>. The location should be fetched automatically 

Alert when we scroll to a specific section in your page. 

Implement a button at the end of the page that will take me to the top of the page when clicked.
